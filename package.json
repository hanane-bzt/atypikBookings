{
  "name": "atypikhouse-ssr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build:all",
    "build:ssr": "cd client && npm run build:ssr",
    "start": "node api/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.3.1",
    "mongoose": "^7.6.1",
    "jsonwebtoken": "^9.0.1",
    "cors": "^2.8.5",
    "cloudinary": "^1.41.3",
    "multer": "^1.4.5-lts.1"
  }
}
